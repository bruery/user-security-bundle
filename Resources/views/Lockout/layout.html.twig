<p class="alert alert-error">{{ 'bruery_user_security.lockout_message'|trans({},'BrueryUserSecurityBundle') }}</p>
{% if show_counter is defined and show_counter %}
    <p id="{{ id }}" class="alert alert-error" style="font-size:20px !important;font-weight:bold;text-align:center;">&nbsp;</p>
    <script type="text/javascript">
        //<![CDATA[
        jQuery(document).ready(function($){
            jQuery('#{{ id }}').countdown({
                interval : {
                    hour   : {{ interval.h}},
                    minute : {{ interval.i}},
                    second : {{ interval.s}}
                },
                callback  : {
                    onEnd : function(e) {
                        $('#{{ id }}').html('{{ 'bruery_user_security.redirecting'|trans({},'BrueryUserSecurityBundle') }}');
                        window.location = "{{ url('fos_user_security_login') }}";
                    }
                },
                format : ['{{ 'bruery_user_security.format.hour_min_sec'|trans({},'BrueryUserSecurityBundle') }}',
                    '{{ 'bruery_user_security.format.min_sec'|trans({},'BrueryUserSecurityBundle') }}',
                    '{{ 'bruery_user_security.format.sec'|trans({},'BrueryUserSecurityBundle') }}']
            });
        });
        //]]>
    </script>
{% endif %}